overwrite: true
schema: "http://localhost:3500/graphql"
documents: null
generates:
  src/__generated__/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
